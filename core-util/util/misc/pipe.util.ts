// import {Identifier} from '@heda/core-util';

// export function asPipe(identifier: Identifier): string {
//   if (!identifier) {
//     return undefined;
//   }
//   return `${identifier.system}|${identifier.value}`;
// }
//
// export function fromPipe(pipeString: string): Identifier {
//   if (!pipeString) {
//     return undefined;
//   }
//   let system = null;
//   let value = pipeString;
//   const pipeIdx = pipeString.indexOf('|');
//   if (pipeIdx && pipeIdx > -1) {
//     system = pipeString.substring(0, pipeIdx);
//     value = pipeString.substring(pipeIdx + 1, pipeString.length);
//   }
//   return {system: system, value: value};
// }




